<%= link_to "Add a category", new_category_path %>
<ul>
  <% @categories.each do |category| %>
    <li>
      <%= category.name %>
      <%= link_to "delete", category_path(category), :data => {:method => :delete} %>
    </li>
  <% end %>
</ul>
